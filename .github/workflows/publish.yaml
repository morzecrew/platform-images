name: Build and Publish Images

on:
  push:
    branches:
      - main
    paths:
      - docker-bake.hcl
      - images/**
      - justfile
      - .github/workflows/publish.yaml
      - "!images/**/README.md"
      - "!README.md"

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-images
  cancel-in-progress: false

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    name: Build and Publish Images
    
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # Publishing authenticates with the explicit GH_TOKEN below; the
          # checkout token is never used, and `just publish` runs no git
          # commands, so persisting it only widens what a build step can read.
          persist-credentials: false

      - name: Install just
        uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3 # v4.0.0
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      # GIT_REVISION / BUILD_DATE / BUILD_STAMP default to empty in
      # docker-bake.hcl so local builds stay cache-stable; CI fills them in.
      #
      # BUILD_STAMP must be unique per *build*, not per run (RFC 0002 row 11).
      # Neither run_id nor run_number changes when a run is re-run -- only
      # run_attempt does -- and a re-run rebuilds from a moved apt/base state,
      # so without it a second attempt repoints a tag the policy calls
      # immutable. run_number is rejected outright: it is stable across re-runs
      # and also resets when a workflow is renamed.
      - name: Publish images
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GIT_REVISION: ${{ github.sha }}
        run: |
          BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
          BUILD_STAMP="$(date -u +%Y%m%d)-${{ github.run_id }}.${{ github.run_attempt }}" \
          just publish