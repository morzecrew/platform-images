# pg_stat_statements ships with the server and is always preloaded.
# Lives here rather than in postgresql.conf: that file is read before conf.d,
# so settings left there would override the generated extension config.
pg_stat_statements.max = 10000
pg_stat_statements.track = all
